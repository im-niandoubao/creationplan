{
  "name": "creationplan-server",
  "version": "1.0.0",
  "private": true,
  "description": "造物计划进度条后端 — 独立 Node 进程，port 3002",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
